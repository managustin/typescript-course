// Literal is a textual representation (notation) of a value as it is written in source code. E.g.: 23, 'hello', True.

let direction: "north" | "south" | "east" | "west"; // these are the values that can be assigned to the direction variable;

direction = 'south';
// direction = 'hello'; //doesn't work

let responseCode: 200 | 404 | 201;
responseCode = 200;
let responseCode1: true;
responseCode1 = false; 